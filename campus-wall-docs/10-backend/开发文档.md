# campus_wall 开发文档

## 项目简介

campus_wall 是校园墙项目的核心业务后端服务，负责处理所有业务逻辑，包括用户认证、帖子管理、评论系统、AI 聊天代理、内容审核、私信系统等。

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 17 | 开发语言 |
| Spring Boot | 3.4.2 | Web 框架 |
| Spring AI | 1.0.8 | LLM 集成（starter `spring-ai-starter-model-openai`，BOM `spring-ai-bom:1.0.8`）|
| Spring WebSocket | - | 实时通讯 |
| Spring AOP | - | 管理端审计切面（@OperationLog）|
| Bucket4j | 8.10.1 | 接口限流（bucket4j-core）|
| Actuator + Micrometer | - | 监控埋点（micrometer-registry-prometheus，暴露 /actuator/prometheus）|
| MyBatis-Plus | 3.5.9 | ORM |
| MySQL | 8.0 | 关系数据库 |
| Redis | 7.0 | 缓存/会话 |
| MinIO | 8.5.7 | 对象存储 |
| JWT | 0.11.5 | 身份认证 |
| spring-security-crypto | - | BCrypt 密码哈希（管理员账号）|
| Flyway | - | 数据库迁移（flyway-core + flyway-mysql）|
| spring-dotenv | 4.0.0 | .env 环境变量加载 |
| Lombok | 1.18.38 | 代码简化 |

## 目录结构

```
campus_wall/
├── src/main/java/com/jyu/campus/
│   ├── CampusWallBackendApplication.java    # 启动类
│   ├── admin/                               # 管理模块（独立 admin_user RBAC 体系）
│   │   ├── controller/                      # 登录、审核、举报、反馈、运营看板、用户/角色/权限、系统配置、审计、学生认证复核
│   │   ├── rbac/                            # RBAC：AdminAuthInterceptor、@RequirePermission、AdminJwtUtil
│   │   ├── audit/                           # 审计：@OperationLog AOP 切面 → admin_oper_log
│   │   ├── stat/                            # 运营看板统计（overview/trend/active/distribution/top-posts）
│   │   ├── sysconfig/                       # 系统配置（sys_config）
│   │   ├── usermgmt/                        # C 端用户管理（封禁/解封）
│   │   ├── report/                          # 举报处理
│   │   ├── feedback/                        # 意见反馈处理
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   ├── ai/                                  # AI 模块（单一入口 Agent；生产默认转发 Python LangGraph 服务）
│   │   ├── agent/                           # Java 进程内 Planner-Executor 兜底引擎（remote-enabled=false 时启用）
│   │   │   ├── controller/                  # AgentController（/agent + AI 发帖 /posts/draft|resume|cancel）
│   │   │   ├── engine/                      # Planner / ToolExecutor / Synthesizer / RelevanceJudge / QuestionContextualizer
│   │   │   ├── tool/                        # ToolRegistry / SearchPostsTool / KnowledgeQaTool / Observation
│   │   │   ├── plan/                        # 计划 DTO（Plan / Step）
│   │   │   └── service/                     # AgentService 编排
│   │   ├── memory/                          # 对话记忆：ConversationMemoryService（短期）+ AiUserMemoryService / MemoryExtractor（长期）
│   │   ├── controller/                      # AiSeniorController(knowledge/history)、AiPreferenceController
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   ├── common/                              # 公共模块
│   │   ├── constant/                        # 常量
│   │   ├── enums/                           # 枚举
│   │   ├── exception/                       # 异常
│   │   ├── result/                          # 统一响应
│   │   └── util/                            # 工具类
│   │       ├── JwtUtil.java                 # JWT 工具
│   │       ├── RedisUtil.java               # Redis 工具
│   │       └── MinioUtil.java               # MinIO 工具
│   ├── community/                           # 社区模块
│   │   ├── controller/                      # 帖子、评论、分类、搜索、排行榜、组队
│   │   ├── enums/                           # BoardType 板块枚举等
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   ├── verification/                        # 学生认证模块（学生证 OCR + AI 置信度 + 人工复核状态机）
│   │   ├── controller/
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   ├── config/                              # 配置类
│   │   ├── MybatisPlusConfig.java
│   │   ├── RedisConfig.java
│   │   ├── WebSocketConfig.java
│   │   └── CorsConfig.java
│   ├── infrastructure/websocket/            # WebSocket 基础设施
│   ├── social/                              # 社交模块
│   │   ├── controller/                      # 用户、关注、私信、通知
│   │   ├── service/
│   │   ├── mapper/
│   │   └── entity/
│   └── task/                                # 定时任务
│       ├── MetricsCollectionTask.java
│       └── FileCleanupTask.java
│
├── src/main/resources/
│   ├── application.yaml                     # 主配置（含 redis-cache 缓存 TTL）
│   ├── application-dev.yaml                 # 开发配置（dev_a/dev_b 各连内网中间件）
│   ├── application-test.yaml                # 测试配置（campus_wall_test / Redis DB15 / bucket campus-test）
│   ├── application-prod.yaml                # 生产配置
│   ├── db/migration/                        # Flyway 迁移
│   └── sensitive-words.txt                  # 敏感词库
│
├── src/test/                                # 测试代码
├── pom.xml
└── docker-compose.yml                       # 基础设施
```

## 开发环境搭建

### 1. 环境要求

- JDK 17+
- Maven 3.8+（或 IDEA 内置）
- MySQL 8.0
- Redis 7.0
- MinIO

### 2. 启动基础设施

```bash
cd campus-wall-ops
docker-compose up -d mysql redis minio
```

### 3. 配置环境变量

```bash
cd campus_wall
cp .env.example .env
# 编辑 .env，配置数据库、Redis、MinIO 连接信息
```

### 4. IDEA 启动

1. IDEA 打开 `campus_wall` 文件夹
2. 等待 Maven 依赖下载
3. 运行 `CampusWallBackendApplication`
4. 访问 `http://localhost:8080/actuator/health`

### 5. 命令行启动

```bash
./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
```

## 核心模块说明

### 1. 认证模块 (common/util/JwtUtil + social)

- **JwtUtil**：JWT Token 生成与验证
- **Interceptor**：请求拦截，验证 Token
- 微信 OAuth2 登录流程：前端获取 code → 后端调用微信接口换取 openid → 生成 JWT Token

### 2. 社区模块 (community)

- **PostController**：帖子 CRUD、点赞、收藏、热帖、组队加入/退出
- **CommentController**：评论发表、列表、删除
- **CategoryController**：分类管理
- **SearchController**：帖子搜索
- **RankController**：排行榜（热帖榜 + 热搜榜）

核心实体：
- `Post`：帖子（id, userId, categoryId, boardType, title, content, images, viewCount, likeCount, status, is_top, price, salary, info_fee, contact, banner_object_name, is_sold...）
- `Comment`：评论（id, postId, userId, content, parentId...）
- `Category`：分类（id, name, sortOrder...）

**板块分类 `BoardType` 枚举（5 值，community/enums）**：

| 枚举 | code | 名称 | 约束 |
|------|------|------|------|
| RECOMMEND | recommend | 推荐 | - |
| SECONDHAND | secondhand | 二手交易 | requirePrice（price=NULL 表面议；is_sold 已售置灰）|
| PARTTIME | parttime | 兼职发布 | requireSalary（salary、info_fee 信息费）|
| PROMOTION | promotion | 推广 | banner_object_name 推广 Banner 大图 |
| TEAM | team | 组队 | team_member / team_max_members |

发现页区域筛选：`GET /api/v1/posts/list` 支持 `scope`/`campus`/`regionScope`/`locationKeywords` 参数（基于 post_campus 表）。

### 3. AI 模块 (ai)

AI 已收口为**单一入口 Agent**。**生产主路径**：当 `ai.agent.remote-enabled=true`（dev/test/prod 默认 `true`）时，`/agent` 转发到部署在 A机(`172.21.160.212`) 的 **Python LangGraph 微服务**（`ai.service.base-url`，默认 `http://172.21.160.212:8011`，即 campus-wall-ai）；Java 端为已认证 `userId` 签发 JWT 随请求转发，Python 共享 `JWT_SECRET` 验签解出 userId。该 Agent 实为 **LangGraph（Plan-Execute + 单次条件 replan）**，Embedding=bge-m3/1024（锁定），Chat=内网 Qwen3.6-35B（`.101:8003`）。设 `ai.agent.remote-enabled=false` 时回退到下文的 **Java 进程内 Planner-Executor-Synthesizer 引擎（本地兜底路径）**，配备反幻觉判定闸与双层对话记忆。旧的 `ChatController` / `SeniorAssistantService` 及"接收问题→保存→调 GraphRAG /query→保存回复"的线性流程已下线。

- **AgentController**（`/api/v1/ai-senior`，唯一对话 + AI 发帖入口）：
  - `POST /agent`：对话入口。`@RequestAttribute("userId")` 强制 JWT 鉴权（不再从 DTO 取 userId）。返回 `{success, answer, posts(匹配帖子卡片), conversationId}`；图片(`images` object name)透传由 Python 侧 VLM 看图说话。
  - `POST /posts/draft`：AI 发帖·出草稿（文本 + 已上传图片 object name → Python 子图经 VLM 理解生成草稿后中断返回草稿卡片，等待用户确认/修改）。
  - `POST /posts/resume/{draftId}`：AI 发帖·续接，对草稿执行 编辑(`edit`)/发布(`publish`)/取消(`cancel`)；发布时以转发的用户 JWT 为身份，由 Python 回调 Java `/posts/publish` 真正落帖（复用审核/限流）。
  - `DELETE /posts/draft/{draftId}`：AI 发帖·丢弃草稿。
- **AiSeniorController**：现仅剩 `knowledge/*`（import / batch-import / list / {id} 删 / import-text / clear）+ `history/*`（list / detail）。
- **AiPreferenceController**：`GET /ai-preference/get`、`POST /ai-preference/save`、`GET /ai-preference/system-prompt`。

**Java 兜底引擎编排流程（`ai.agent` 包：engine / tool / plan / service；`remote-enabled=false` 时启用）**：
1. 读短期记忆（`ConversationMemoryService`，落 `ai_chat_record`）
2. 指代消解（`QuestionContextualizer`，补全"它/那个"等指代）
3. 规划（`Planner`，决定调用哪些只读工具）
4. 执行工具（`ToolExecutor` → `ToolRegistry`：`SearchPostsTool` 调 campus-wall-ai（端口 8011，原 graphrag 服务已并入）`POST /match-posts` 找帖；`KnowledgeQaTool` 调 campus-wall-ai `POST /query` 查知识库）
5. **反幻觉判定（`RelevanceJudge`）**：用 Spring AI 结构化输出收 `List<Long>`（裸 postId 数组），从严判定只放行真正相关的帖子，治好了 match-posts 把无关帖当匹配的瞎答问题
6. 严格接地合成（`Synthesizer`，只基于判定通过的证据生成答案）
7. 异步沉淀长期记忆（`ai.memory`：`MemoryExtractor.extractAsync` 抽取，存 `ai_user_memory` 表，由 `AiUserMemoryService` 管理）

**与 campus-wall-ai 的交互**（原 graphrag 服务已并入 campus-wall-ai，端口 8011）：agent 经 `GraphRagBackend`（实现 `IRagBackend`，`graphrag.base-url` 默认 `http://localhost:8011`）调用 `POST /match-posts`（找帖）与 `POST /query`（知识库问答）；发帖时异步入图谱 `POST /ingest-post`；知识库导入 `POST /index`。

### 4. 社交模块 (social)

- **UserController**：用户信息、登录
- **FollowController**：关注/取消关注
- **MessageController**：私信（REST API + WebSocket）
- **NotificationController**：消息通知

WebSocket 私信：
- 连接地址：`ws://host/ws/chat`
- 认证方式：Header 携带 `Authorization: Bearer <token>`
- 消息格式：`{ type, toUserId, content, timestamp }`

### 5. 管理模块 (admin)

管理端为**物理隔离的独立 `admin_user` 账号体系 + RBAC**（不是给 C 端 `user` 加 `role` 字段）。后端是鉴权权威。

**RBAC 模型（V4.0/V4.1）**：5 张表 `admin_user` / `admin_role` / `admin_permission` / `admin_user_role` / `admin_role_permission`；3 个预置角色：`superadmin`（全权）/ `moderator`（内容审核员）/ `viewer`（只读运营）；权限码形如 `module:action`（如 `moderation:queue`、`stat:view`、`user:ban`、`role:assign`、`sys:config`、`audit:view`，以及 V4.8 新增的知识库管理 `knowledge:list` / `knowledge:import` / `knowledge:delete`）。

**鉴权链路**：`AdminAuthInterceptor` + 方法级 `@RequirePermission(权限码)` + `AdminJwtUtil`（独立 admin token，与 C 端 JWT 隔离）。

**Controller**：
- **AdminAuthController**：`POST /admin/login`（查 `admin_user` 签发 admin token，BCrypt 校验）、`GET /admin/auth/me`、`POST /admin/auth/logout`。登录已从 `AdminModerationController` 迁出。
- **AdminModerationController**：`moderation/queue`、`scan`、`{id}/approve`、`{id}/reject`、`GET /admin/stats`（瞬时卡片，与运营看板 stat/* 并存），均需 `@RequirePermission`。
- **AdminReportController**：举报处理。
- **AdminFeedbackController**：意见反馈处理。
- **AdminStudentVerificationController**：学生认证人工复核（`/admin/verification` list / review）。
- **AdminUserMgmtController**：`/admin/users` C 端用户封禁/解封。
- **AdminManageController**：管理员/角色/权限 CRUD（`/admin/admins`、`/roles`、`/permissions`、`/roles/{id}/permissions`）。
- **StatController**（运营看板）：`/admin/stat/{overview,trend,active,distribution,top-posts}`（`stat:view`）。配 `stat_daily` 每日快照表（V4.3，DAU 用 HLL 去重）。
- **AdminSysConfigController**：`GET/PUT /admin/configs`（`sys:config` / `sys:config-save`）。配 `sys_config` 表（V4.4）。
- **AuditLogController**：`GET /admin/audit`（`audit:view`）。
- **AdminKnowledgeController**（知识库管理，监控面板调用）：前缀 `/api/v1/admin/knowledge`：`POST /import`（`knowledge:import`，上传文件或粘贴文本，转发 AI 服务 `/ingest` 服务端切割）、`GET /docs`（`knowledge:list`）、`DELETE /docs/{docId}`（`knowledge:delete`，连同图谱/向量节点）。权限码 V4.8 新增。

**操作审计**：`@OperationLog` AOP 切面（`admin.audit` 包）拦截管理操作 → 写入 `admin_oper_log` 表（V4.2）。

> ⚠️ 旧文档"管理员缺角色体系 / 无 `User.role` 鉴权 / 任何登录用户可访问管理接口"的说法已全部作废。

### 6. 内容审核

三层审核机制：
1. **敏感词过滤**：加载 `sensitive-words.txt`，DFA 算法匹配
2. **AI 复审**：命中可疑规则时，调用本地 `qwen2.5:7b` 判定
3. **人工审核**：AI 不确定的内容进入管理后台审核队列（`@RequirePermission(moderation:queue)`）

配置项（`application.yaml`）：
```yaml
moderation:
  sensitive-words-path: classpath:sensitive-words.txt
  ai-rescore-enabled: true
```

### 7. 缓存模块 (common/util/RedisUtil)

缓存策略：

| 缓存 Key | 过期时间 | 说明 |
|----------|----------|------|
| `user:info:{userId}` | 2小时 | 用户信息 |
| `post:detail:{postId}` | 30分钟 | 帖子详情 |
| `post:list:{category}:{page}` | 5分钟 | 帖子列表 |
| `comment:list:{postId}` | 10分钟 | 评论列表 |
| `hot:posts` | 10分钟 | 热帖列表 |
| `user:stats:{userId}` | 30分钟 | 用户统计 |

### 8. 定时任务 (task)

- **MetricsCollectionTask**：收集业务指标（ Prometheus 自定义指标）
- **FileCleanupTask**：清理过期临时文件

## 配置说明

### application.yaml（主配置）

```yaml
spring:
  application:
    name: campus-wall-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

flyway:
  enabled: true
  locations: classpath:db/migration
  baseline-on-migrate: true
```

### application-dev.yaml（开发配置）

> 注：`redis-cache`（缓存开关 + 各 Key TTL）在主配置 `application.yaml`，**不在** dev 配置。dev 默认直连 A机(GB10/ 172.21.160.212) 的中间件；dev_a / dev_b 经 `REDIS_DB` 环境变量分别用逻辑库 1 / 2。

```yaml
spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://172.21.160.212:3306/campus_wall?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST:172.21.160.212}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
      database: ${REDIS_DB:0}        # dev_a/dev_b 经 REDIS_DB 改为 1/2
```

### application-test.yaml（测试配置，全队共享的"验收台"）

```yaml
spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://172.21.160.212:3306/campus_wall_test?...&createDatabaseIfNotExist=true}
  data:
    redis:
      database: ${REDIS_DB:15}       # test 专用逻辑库
minio:
  bucket-name: ${MINIO_BUCKET:campus-test}
```

启动：`SPRING_PROFILES_ACTIVE=test`（IDEA 运行配置 Active profiles 填 test）。test 库 `campus_wall_test`、Redis 逻辑库 15、MinIO bucket `campus-test`，与 dev/prod 隔离。

### 三环境隔离（dev / test / prod）

A机（GB10 / `172.21.160.212`，主机名 spark-ab42）集中所有中间件 + AI 服务容器 + 模型；B机为开发机，跑 Java 后端 + 前端，连 A机。团队约定：dev 每人一套，test/prod 全队共享。

| 维度 | prod | dev_a | dev_b | test |
|------|------|-------|-------|------|
| `SPRING_PROFILES_ACTIVE` | prod | dev | dev | test |
| MySQL 库 | `campus_wall` | `campus_wall_dev_a` | `campus_wall_dev_b` | `campus_wall_test` |
| Redis 逻辑库（`REDIS_DB`）| 0 | 1 | 2 | 15 |
| MinIO bucket | `campus-wall` | `campus-dev-a` | `campus-dev-b` | `campus-test` |

MinIO bucket 由 Java `MinioUtil` 启动时自动创建。AI 服务侧切环境用 `APP_ENV=dev/test/prod`（`config.py` 自动加载 `.env.<env>`）。

### 环境变量

| 变量 | 说明 | 示例 |
|------|------|------|
| `SPRING_PROFILES_ACTIVE` | 激活的配置文件 | `dev` / `prod` |
| `MYSQL_PASSWORD` | MySQL 密码 | `123456` |
| `REDIS_PASSWORD` | Redis 密码 | - |
| `MINIO_ENDPOINT` | MinIO 地址 | `http://localhost:9000` |
| `MINIO_ACCESS_KEY` | MinIO 用户名 | `root` |
| `MINIO_SECRET_KEY` | MinIO 密码 | - |
| `WECHAT_APP_ID` | 微信 AppID | - |
| `WECHAT_APP_SECRET` | 微信 AppSecret | - |
| `DASHSCOPE_API_KEY` | DashScope API Key（云端模型）| - |
| `GRAPHRAG_BASE_URL` | campus-wall-ai 服务地址（`graphrag.base-url`；原 GraphRAG 服务已并入 campus-wall-ai:8011）| `http://localhost:8011` |

### 数据库与迁移（Flyway）

- 迁移脚本 **V3.0 ～ V4.8 共 19 个**（基线已重置，不存在 V1.0/V1.1/V2.0/V2.1；`V3.0__baseline` 含 22 表）：
  - V3.0 基线、V3.1 初始化数据、V3.2 板块重映射、V3.3 post 板块字段、V3.4 标签表、V3.5 team_member、V3.6 team_max_members、V3.7 post_campus、V3.8 排行榜+学生认证、V3.9 ai_user_memory、V4.0 admin_rbac、V4.1 rbac_init、V4.2 admin_audit_report_user、V4.3 stat_daily、V4.4 sys_config。
  - V4.5 ai_post_draft（AI 发帖草稿表，Python 侧 SQLAlchemy 读写、Java 无实体/Mapper）、V4.6 ai_user_memory 加 content_hash + `(user_id, content_hash)` 唯一键（跨语言写入幂等）、V4.7 ai_chat_record 补 message_type / metadata_json / token_count 结构化列、V4.8 admin 知识库权限码（knowledge:list / import / delete，并分配给 superadmin/moderator/viewer）。
- **当前共约 38 张表**（V4.5 新增 ai_post_draft；V4.6/V4.7 为加列、V4.8 为插权限数据，不新增表）。
- 脚本采用 **MySQL 8 安全幂等写法**（`information_schema` + `PREPARE` 守卫；MySQL 不支持 `ADD COLUMN/INDEX ... IF NOT EXISTS`）。

## 接口列表

### 帖子相关 (/api/v1/posts)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 帖子列表（支持分类、分页；区域筛选参数 scope/campus/regionScope/locationKeywords）|
| GET | `/hot` | 热帖列表 |
| GET | `/leaderboard` | 按浏览量排行（旧接口，仍在）|
| GET | `/detail/{id}` | 帖子详情 |
| POST | `/publish` | 发布帖子（按 boardType 校验 price/salary 等字段）|
| POST | `/like/{id}` | 点赞/取消点赞 |
| POST | `/collect/{id}` | 收藏/取消收藏 |
| POST | `/{id}/join` | 加入组队（TEAM 板块）|
| POST | `/{id}/leave` | 退出组队（TEAM 板块）|
| DELETE | `/delete/{id}` | 删除帖子 |

### 评论相关 (/api/v1/comments)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 评论列表 |
| POST | `/add` | 发表评论 |
| DELETE | `/delete/{id}` | 删除评论 |

### 用户相关 (/api/v1/users)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/login` | 微信登录 |
| GET | `/info` | 获取用户信息 |
| PUT | `/update` | 更新用户信息 |

### AI 相关 (/api/v1/ai-senior、/api/v1/ai-preference)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/ai-senior/agent` | **唯一对话入口**（默认转发 Python LangGraph 服务，`remote-enabled=false` 走 Java 兜底）|
| POST | `/ai-senior/posts/draft` | AI 发帖·出草稿 |
| POST | `/ai-senior/posts/resume/{draftId}` | AI 发帖·续接（edit / publish / cancel）|
| DELETE | `/ai-senior/posts/draft/{draftId}` | AI 发帖·丢弃草稿 |
| GET | `/ai-senior/history/list` | 对话历史列表 |
| GET | `/ai-senior/history/detail` | 对话详情 |
| POST | `/ai-senior/knowledge/import` | 知识库单条导入 |
| POST | `/ai-senior/knowledge/batch-import` | 知识库批量导入 |
| POST | `/ai-senior/knowledge/import-text` | 知识库文本导入 |
| GET | `/ai-senior/knowledge/list` | 知识库条目列表 |
| DELETE | `/ai-senior/knowledge/{id}` | 删除知识库条目 |
| POST | `/ai-senior/knowledge/clear` | 清空知识库 |
| GET | `/ai-preference/get` | 获取 AI 偏好 |
| POST | `/ai-preference/save` | 保存 AI 偏好 |
| GET | `/ai-preference/system-prompt` | 获取系统提示词 |

> 旧端点 `/ai-senior/chat`、`/chat/stream`、后端侧 `/match-posts`，以及 `/api/v1/ai/chat` 概念均已下线。

### 排行榜 (/api/v1/ranks)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/posts` | 热帖榜（period=day/week/month；scope=0本校/1跨校/2全省；limit≤50）|
| GET | `/search-hot` | 热搜榜（24h / 7天 / 30天）|

配 `hot_rank_config` 表（V3.8）。

### 学生认证 (/api/v1/verification)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/upload` | 上传学生证（OCR 正/反面 MinIO 对象名）|
| POST | `/submit` | 提交认证申请 |
| GET | `/status` | 查询认证状态 |

配 `student_verification` 表（V3.8，AI 置信度 + 人工复核状态机），`user` 加 `student_verified` / `student_no`。

### 管理后台 (/api/v1/admin)

> 全部需 admin token + `@RequirePermission`（登录除外）。

| 方法 | 路径 | 权限码 | 说明 |
|------|------|--------|------|
| POST | `/admin/login` | - | 管理员登录（签发 admin token）|
| GET | `/admin/auth/me` | - | 当前管理员信息 |
| POST | `/admin/auth/logout` | - | 登出 |
| GET | `/admin/moderation/queue` | moderation:queue | 待审核队列 |
| POST | `/admin/moderation/{id}/approve` | moderation:queue | 审核通过 |
| POST | `/admin/moderation/{id}/reject` | moderation:queue | 审核拒绝 |
| GET | `/admin/stats` | stat:view | 瞬时统计卡片 |
| GET | `/admin/stat/overview` | stat:view | 运营看板：KPI 概览 |
| GET | `/admin/stat/trend` | stat:view | 运营看板：趋势 |
| GET | `/admin/stat/active` | stat:view | 运营看板：活跃度 |
| GET | `/admin/stat/distribution` | stat:view | 运营看板：分布 |
| GET | `/admin/stat/top-posts` | stat:view | 运营看板：热帖 Top |
| GET/PUT | `/admin/configs` | sys:config / sys:config-save | 系统配置 |
| GET | `/admin/audit` | audit:view | 操作审计日志 |
| POST | `/admin/knowledge/import` | knowledge:import | 知识库导入（文件/文本→AI 服务 /ingest）|
| GET | `/admin/knowledge/docs` | knowledge:list | 知识库文档列表 |
| DELETE | `/admin/knowledge/docs/{docId}` | knowledge:delete | 删除知识库文档 |
| - | `/admin/users` | user:ban | 用户封禁/解封 |
| - | `/admin/admins`、`/roles`、`/permissions` | role:assign 等 | 管理员/角色/权限 CRUD |
| - | `/admin/verification` | - | 学生认证人工复核（list / review）|

## 测试

```bash
# 运行所有测试
mvn test

# 运行单个测试类
mvn test -Dtest=PostServiceTest

# 测试覆盖率
mvn jacoco:report
```

## 构建与部署

```bash
# 打包
mvn clean package -DskipTests

# 生成 jar 包在 target/ 目录
# campus-wall-0.0.1-SNAPSHOT.jar

# 运行 jar
java -jar target/campus-wall-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod
```

### systemd 部署

参考 `campus-wall-ops/deploy/campus-wall.service`：

```ini
[Unit]
Description=Campus Wall Backend
After=network.target

[Service]
Type=simple
User=campus
WorkingDirectory=/opt/campus-wall
ExecStart=/usr/bin/java -jar campus-wall.jar --spring.profiles.active=prod
Restart=always

[Install]
WantedBy=multi-user.target
```

## 常见问题

### Q: Maven 依赖下载慢？

在 `pom.xml` 或 `~/.m2/settings.xml` 中配置阿里云镜像：

```xml
<mirror>
    <id>aliyunmaven</id>
    <mirrorOf>*</mirrorOf>
    <name>阿里云公共仓库</name>
    <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### Q: Flyway 迁移失败？

1. 检查数据库连接是否正常
2. 检查 `db/migration/` 下的 SQL 语法
3. 如需重置，删除 Flyway 历史表：`DELETE FROM flyway_schema_history;`

### Q: Redis 连接失败？

检查 `application-dev.yaml` 中的 Redis 配置，确保密码正确。

### Q: 上传图片失败？

1. 检查 MinIO 是否正常运行
2. 检查 MinIO 配置（endpoint, accessKey, secretKey）
3. 检查 bucket 是否已创建
