# 校园墙热榜 / AI 偏好 / 学生认证开发文档

> 版本：v1.0  
> 适用范围：后端（Spring Boot 3.4 + MyBatis-Plus + Redis）+ 前端（uni-app Vue3 + TypeScript）  
> 目标：补齐用户自定义 AI 偏好生效链路、热帖榜、热搜榜、学生认证四大能力。

---

## 1. 需求概述

| 模块 | 当前现状 | 目标 |
|------|----------|------|
| AI 偏好设置 | 表 `ai_preference`、接口 `/api/v1/ai-preference/*` 已存在，且前端 `pages/ai/preference.vue` 已可配置。 | **AI 对话（Agent）调用时读取该偏好并注入系统提示词**，让配置真正生效。 |
| 热帖榜 | 存在 `GET /api/v1/posts/hot`（按点赞+浏览量排序）、`GET /api/v1/posts/leaderboard`（按浏览量排序），但无时间窗口、无自动淘汰。 | 实现 **日榜 / 周榜 / 月榜**，按**实时浏览量+互动量+时间衰减**计算热度，过期自动清除，体验对齐微博热搜。 |
| 热搜榜 | 存在 `GET /api/v1/search/hot`，基于 Redis ZSet 累计搜索次数，过期 7 天。 | 实现 **近 24h / 7 天 / 30 天滑动窗口**，自动清除旧搜索，带“热/新/爆”标签。 |
| 学生认证 | 无相关表与接口。 | 用户上传学生证/一卡通正反面，**AI OCR 初筛 + 人工复核**，通过后给用户打认证标识。 |

---

## 2. 总体架构

```text
┌─────────────────────────────────────────────────────────────┐
│                        前端 (uni-app)                        │
│  热帖页  │  搜索页  │  AI偏好页  │  学生认证页  │  管理端   │
└─────────┴─────────┴───────────┴─────────────┴──────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                       后端 (Spring Boot)                     │
│  PostRankController  SearchController  AgentController       │
│      │                     │                  │             │
│  PostRankService     SearchHotService    AgentService        │
│      │                     │                  │             │
│  Redis ZSet + DB     Redis ZSet + DB   AI Preference Prompt  │
│      │                     │                                │
│  Scheduled Task      Scheduled Task                          │
│  (热度聚合/过期清理)  (窗口滑动/过期清理)                      │
└─────────────────────────────────────────────────────────────┘
```

核心设计原则：
1. **读多写少**：榜单读走 Redis ZSet，写只更新计数或分数，异步任务定期聚合。
2. **时间窗口**：热帖、热搜均使用 Redis 过期 + 滑动窗口，到点自动失效。
3. **最小入侵**：复用现有 `PostVO`、`Result`、`RequestContext`、`MinioUtil` 等基础设施。
4. **认证安全**：证件图片使用 MinIO 私有路径 + 后端代理访问，OCR 结果仅用于初筛，关键决策由人工复核。

---

## 3. AI 偏好设置生效（修复现有链路）

### 3.1 现状问题

- `AiPreferenceController` 提供 `GET /api/v1/ai-preference/get`、`POST /api/v1/ai-preference/save`、`GET /api/v1/ai-preference/system-prompt`。
- `AgentController`（当前 AI 对话入口 `POST /api/v1/ai-senior/agent`）**未读取用户偏好**，`Synthesizer` 使用的是固定 `SYSTEM_PROMPT`。

### 3.2 改动方案

1. 在 `AgentServiceImpl.handle()` 中调用 `IAiPreferenceService.buildSystemPrompt(userId)`。
2. 将偏好提示词追加到 `Synthesizer` 的 system prompt 中（保留原有的“严格接地”规则）。
3. `AgentController` 从 `@RequestAttribute("userId")` 获取当前用户，避免前端在 DTO 里显式传 `userId`（兼容老字段，后端优先取 token 中的 userId）。

### 3.3 接口调整

#### `POST /api/v1/ai-senior/agent`（已有）

**请求体（兼容现有，userId 改为可选）**：
```json
{
  "question": "我丢了一个蓝色书包",
  "conversationId": ""
}
```

**说明**：后端优先从 JWT / Cookie 解析 `userId`，`conversationId` 为空时后端生成 UUID。

**响应不变**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "success": true,
    "answer": "...",
    "conversationId": "uuid",
    "posts": [...],
    "plan": {...}
  }
}
```

### 3.4 后端代码变更点

- `AgentController.agent`：增加 `@RequestAttribute("userId") Long userId`，优先使用 token 中的 userId。
- `AgentServiceImpl.handle`：签名改为 `handle(Long userId, String question, String conversationId)`，注入偏好 prompt。
- `Synthesizer.synthesize`：增加 `String userPreferencePrompt` 参数，或在构造 system prompt 时拼接。
- 偏好 prompt 示例：
  ```
  你是嘉应学院的 AI 学长助手。
  【性格特征】你是一位友好亲切、热情助人的学长。
  【语言风格】使用轻松、随意的日常交流语气。
  【专业领域】提供通用知识和校园生活指导。
  【回复长度】回答要适中详细，提供必要的解释和举例。
  【特殊要求】...(用户自定义)
  ```

---

## 4. 热帖榜（日榜 / 周榜 / 月榜）

### 4.1 算法设计：综合热度分 + 滑动窗口

热度分公式（参考 Hacker News / 微博热搜）：

```
score = (viewWeight * viewCount + likeWeight * likeCount
         + commentWeight * commentCount + collectWeight * collectCount)
        / pow(hoursSincePublish + 2, gravity)
```

建议参数（可配置到 `application.yaml`）：
- `viewWeight = 1`
- `likeWeight = 5`
- `commentWeight = 8`
- `collectWeight = 3`
- `gravity = 1.5`

榜单按 score 倒排。帖子发布时间超过窗口期后自然无法入榜。

### 4.2 数据流

1. **实时写**：用户浏览帖子时，`PostPublishService.incrementViewCount()` 同时给 Redis 计数器 `campus:post:views:{postId}` +1。
2. **定时聚合**：每 5 分钟执行一次 `HotPostRankTask`，计算窗口内帖子 score，写入三个 ZSet：
   - `campus:rank:post:daily`（24h）
   - `campus:rank:post:weekly`（7d）
   - `campus:rank:post:monthly`（30d）
3. **自动过期**：写入时设置 TTL = 窗口期，或任务中用 `ZREMRANGEBYSCORE` 移除过期帖子。
4. **读榜单**：Controller 直接 `ZREVRANGE` 取前 N。

### 4.3 数据库表

**新增配置表**（可选，支持后台动态调整权重）：
```sql
CREATE TABLE IF NOT EXISTS hot_rank_config (
    id              bigint auto_increment primary key,
    config_key      varchar(64)  not null comment '配置项：view_weight/like_weight/...',
    config_value    varchar(64)  not null comment '配置值',
    description     varchar(255) null,
    update_time     datetime default CURRENT_TIMESTAMP on update CURRENT_TIMESTAMP,
    constraint uk_config_key unique (config_key)
) charset = utf8mb4 comment='热榜算法配置表';
```

**初始化数据**：
```sql
INSERT INTO hot_rank_config(config_key, config_value, description) VALUES
('post.view_weight', '1', '浏览量权重'),
('post.like_weight', '5', '点赞权重'),
('post.comment_weight', '8', '评论权重'),
('post.collect_weight', '3', '收藏权重'),
('post.gravity', '1.5', '时间衰减系数'),
('post.rank.limit', '50', '榜单缓存条数');
```

### 4.4 Redis Key 规范

```java
// 帖子实时浏览计数（计数器）
campus:post:views:{postId}

// 三个榜单 ZSet：member=postId, score=热度分
campus:rank:post:daily
campus:rank:post:weekly
campus:rank:post:monthly

// 榜单元数据缓存（更新时间、兜底空值）
campus:rank:post:meta:{period}
```

### 4.5 后端接口

#### `GET /api/v1/ranks/posts`

**说明**：获取热帖榜单，替代或增强现有 `/api/v1/posts/hot`。

**请求参数**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| period | string | 是 | `day` / `week` / `month` |
| limit | int | 否 | 默认 10，最大 50 |
| scope | int | 否 | `0` 本校，`1` 跨校，不传为全部 |

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "period": "day",
    "updateTime": "2026-06-13T10:00:00",
    "list": [
      {
        "rank": 1,
        "postId": 10086,
        "score": 1285.6,
        "viewCount": 520,
        "likeCount": 32,
        "commentCount": 18,
        "collectCount": 5,
        "post": { /* 完整的 PostVO */ }
      }
    ]
  }
}
```

**错误响应**：
```json
{
  "code": 400,
  "message": "不支持的榜单周期",
  "data": null
}
```

### 4.6 前端对接

**新增 API**（`src/api/index.js`）：
```js
export const rankApi = {
  getHotPostRank: (period = 'day', limit = 10, scope) => ({
    url: '/api/v1/ranks/posts',
    method: 'GET',
    params: { period, limit, ...(scope !== undefined ? { scope } : {}) }
  })
}
```

**页面改造**：
- `pages/hot-posts/hot-posts.vue`：顶部增加 `日榜 / 周榜 / 月榜` Tab，调用 `rankApi.getHotPostRank`。
- 保留原有微博热搜式 TOP3 卡片 + 后续列表样式。

---

## 5. 热搜榜

### 5.1 算法设计：滑动窗口 + 搜索次数 + 标签

与现有 `search_record` 表互补：
- **持久化**：仍用 `search_record` 记录总搜索次数（用于长期分析）。
- **实时榜**：用 Redis 滑动窗口 ZSet，按窗口内搜索次数倒排。

实现方式：
1. 每次搜索时给当前时间窗口的 ZSet 加 1 分：
   - `campus:search:hot:{date}`，按天分桶（如 `campus:search:hot:20260613`）。
   - 每个分桶 TTL = 30 天，自动过期。
2. 读取榜单时，按周期合并最近 N 个分桶：
   - 24h：合并今天 + 昨天（按小时比例折算）。
   - 7d：合并最近 7 天。
   - 30d：合并最近 30 天。
3. 合并后计算“热/新/爆”标签：
   - 新：首次进入前 50。
   - 热：搜索次数 > 100。
   - 爆：搜索次数 > 1000 或环比增速 > 300%。

### 5.2 Redis Key 规范

```java
// 按天分桶的热搜 ZSet：member=keyword, score=当天搜索次数
campus:search:hot:yyyyMMdd

// 合并后的榜单缓存（读多写少）
campus:search:rank:day
campus:search:rank:week
campus:search:rank:month

// 昨日榜单缓存（用于环比）
campus:search:rank:yesterday
```

### 5.3 后端接口

#### `GET /api/v1/ranks/search-hot`

**说明**：替代或增强现有 `GET /api/v1/search/hot`。

**请求参数**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| period | string | 是 | `day` / `week` / `month` |
| limit | int | 否 | 默认 10，最大 50 |

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "period": "day",
    "updateTime": "2026-06-13T10:00:00",
    "list": [
      {
        "rank": 1,
        "keyword": "期末复习",
        "searchCount": 1250,
        "type": "爆",
        "trend": "up",
        "changeRank": 5
      },
      {
        "rank": 2,
        "keyword": "二手电瓶车",
        "searchCount": 86,
        "type": "新",
        "trend": "new",
        "changeRank": null
      }
    ]
  }
}
```

字段说明：
- `type`：`热` / `新` / `爆`。
- `trend`：`up` / `down` / `same` / `new`。
- `changeRank`：较昨日榜单排名变化（正数上升，负数下降）。

### 5.4 前端对接

**新增 API**：
```js
export const rankApi = {
  getHotSearchRank: (period = 'day', limit = 10) => ({
    url: '/api/v1/ranks/search-hot',
    method: 'GET',
    params: { period, limit }
  })
}
```

**页面改造**：
- `pages/search/search.vue`：热搜榜单区域增加 `日榜 / 周榜 / 月榜` Tab，调用 `rankApi.getHotSearchRank`。

---

## 6. 学生认证（AI 初筛 + 人工复核）

### 6.1 流程

```
用户上传正面 -> 上传反面 -> 提交认证 -> AI OCR 识别 ->
  ├─ 高置信度通过 -> 待人工复核（异步）-> 管理员通过/驳回
  └─ 低置信度/异常 -> 直接待人工复核
```

状态机：
- `pending` 待提交（用户未提交）
- `submitted` 已提交，等待 AI 初筛
- `ai_passed` AI 初筛通过，等待人工复核
- `ai_failed` AI 初筛失败（自动转人工复核）
- `manual_passed` 人工复核通过（认证成功）
- `manual_rejected` 人工复核驳回

### 6.2 数据库表

```sql
CREATE TABLE IF NOT EXISTS student_verification (
    id                  bigint auto_increment primary key comment '主键ID',
    user_id             bigint          not null comment '用户ID',
    real_name           varchar(50)     null comment '真实姓名（OCR识别）',
    student_no          varchar(50)     null comment '学号（OCR识别）',
    university_name     varchar(100)    null comment '学校名称（OCR识别）',
    college             varchar(100)    null comment '学院（OCR识别）',
    front_object_name   varchar(500)    not null comment '学生证正面 MinIO 对象名',
    back_object_name    varchar(500)    not null comment '学生证反面 MinIO 对象名',
    status              varchar(32)     default 'pending' not null comment '认证状态',
    ai_score            int             null comment 'AI 置信度 0-100',
    ai_reason           varchar(512)    null comment 'AI 初筛说明',
    reviewer_id         bigint          null comment '人工复核管理员ID',
    review_remark       varchar(512)    null comment '人工复核备注',
    submit_time         datetime        null comment '提交时间',
    ai_review_time      datetime        null comment 'AI初筛时间',
    manual_review_time  datetime        null comment '人工复核时间',
    create_time         datetime        default CURRENT_TIMESTAMP not null,
    update_time         datetime        default CURRENT_TIMESTAMP not null on update CURRENT_TIMESTAMP,
    constraint uk_user_id unique (user_id)
) charset = utf8mb4 comment='学生认证表';
```

**用户表扩展字段**：
```sql
ALTER TABLE user
    ADD COLUMN student_verified tinyint(1) default 0 null comment '是否通过学生认证: 0-否 1-是',
    ADD COLUMN student_no varchar(50) null comment '认证通过的学号（脱敏后展示）';
```

### 6.3 文件上传路径

使用 MinIO，路径规范：
```
verification/{userId}/{uuid}_front.{ext}
verification/{userId}/{uuid}_back.{ext}
```

前端先用 `uni.chooseImage` 选图，再调用后端上传接口。

### 6.4 后端接口

#### 6.4.1 上传认证图片

`POST /api/v1/verification/upload`

**Content-Type**：`multipart/form-data`

**请求参数**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | file | 是 | 图片文件 |
| side | string | 是 | `front` 正面 / `back` 反面 |

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "objectName": "verification/10086/xxx_front.jpg",
    "url": "https://.../api/v1/files/view?objectName=..."
  }
}
```

#### 6.4.2 提交认证

`POST /api/v1/verification/submit`

**请求体**：
```json
{
  "frontObjectName": "verification/10086/xxx_front.jpg",
  "backObjectName": "verification/10086/xxx_back.jpg"
}
```

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "verificationId": 1,
    "status": "submitted"
  }
}
```

**说明**：提交后进入异步 AI OCR 初筛（可用队列或 `@Async`）。

#### 6.4.3 查询认证状态

`GET /api/v1/verification/status`

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "manual_passed",
    "statusName": "认证通过",
    "studentNoMask": "2021****1234",
    "realNameMask": "张*",
    "submitTime": "2026-06-13T10:00:00",
    "reviewTime": "2026-06-13T11:00:00",
    "reviewRemark": ""
  }
}
```

#### 6.4.4 管理员复核（管理端）

`POST /api/v1/admin/verification/review`

**请求体**：
```json
{
  "verificationId": 1,
  "action": "pass",
  "remark": "信息清晰，通过"
}
```

`action`：`pass` / `reject`。

**成功响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

#### 6.4.5 管理员列表（管理端）

`GET /api/v1/admin/verification/list`

**请求参数**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | string | 否 | 状态筛选 |
| page | int | 否 | 默认 1 |
| size | int | 否 | 默认 20 |

**成功响应**：分页列表，字段同表结构 + 图片 URL。

### 6.5 AI OCR 初筛

**实现方式**：
- 优先调用本地 / 内网 OCR 服务（如 PaddleOCR / 自己部署的 LLM）。
- 若无本地 OCR，可调用云端 OCR（阿里云、腾讯云等）。
- OCR 结果要求识别出：姓名、学号、学校名。
- 将识别结果与 `user.university_id` 关联的学校名做模糊匹配，给出置信度。

**初筛逻辑**：
- 置信度 >= 80 且学校名匹配：状态 `ai_passed`。
- 置信度 < 80 或学校名不匹配：状态 `ai_failed`（仍进入人工复核）。
- 关键字段缺失：状态 `ai_failed`。

### 6.6 前端对接

**新增 API**（`src/api/index.js`）：
```js
export const verificationApi = {
  uploadImage: (side) => ({
    url: '/api/v1/verification/upload?side=' + side,
    method: 'POST'
  }),
  submit: (data) => ({
    url: '/api/v1/verification/submit',
    method: 'POST',
    data
  }),
  getStatus: () => ({
    url: '/api/v1/verification/status',
    method: 'GET'
  })
}
```

**新增页面**：
- `pages/user/student-verification.vue`：上传正反面、预览、提交。
- 展示认证状态与结果。

---

## 7. 统一接口规范

### 7.1 响应格式

沿用现有 `Result<T>`：
```json
{
  "code": 200,
  "message": "success",
  "data": { }
}
```

### 7.2 分页格式

沿用 MyBatis-Plus `IPage<T>`：
```json
{
  "records": [...],
  "total": 100,
  "size": 10,
  "current": 1,
  "pages": 10
}
```

### 7.3 认证与权限

- 普通用户接口：从 `Authorization: Bearer {token}` 或 HttpOnly Cookie 解析 userId。
- 管理端接口：需要额外判断用户角色（可在现有 `JwtInterceptor` 或 `SecurityConfig` 中扩展）。

---

## 8. 后端任务调度

新增 `RankRefreshTask`：

```java
@Component
public class RankRefreshTask {

    /** 每 5 分钟刷新一次热帖榜 */
    @Scheduled(fixedRate = 5 * 60 * 1000)
    public void refreshPostRank() { ... }

    /** 每 10 分钟刷新一次热搜榜 */
    @Scheduled(fixedRate = 10 * 60 * 1000)
    public void refreshSearchRank() { ... }
}
```

---

## 9. 前端改动清单

| 页面/文件 | 改动 |
|-----------|------|
| `src/api/index.js` | 新增 `rankApi`、`verificationApi`；保留并修正 `searchApi.getHotSearch`。 |
| `src/pages/ai/chat.vue` | 确保调用 `ai-senior/agent` 时无需传 userId。 |
| `src/pages/ai/preference.vue` | 无需大改，已存在。 |
| `src/pages/hot-posts/hot-posts.vue` | 增加 `日榜 / 周榜 / 月榜` Tab，调用 `/api/v1/ranks/posts`。 |
| `src/pages/search/search.vue` | 热搜增加 `日榜 / 周榜 / 月榜` Tab，调用 `/api/v1/ranks/search-hot`。 |
| `src/pages/user/settings.vue` | 增加“学生认证”入口。 |
| `src/pages/user/student-verification.vue` | 新增认证页面。 |

---

## 10. 后端改动清单

| 文件 | 改动 |
|------|------|
| `ai/agent/controller/AgentController.java` | userId 改为从 `@RequestAttribute` 获取。 |
| `ai/agent/service/impl/AgentServiceImpl.java` | 注入 AI 偏好 prompt。 |
| `ai/agent/engine/Synthesizer.java` | system prompt 拼接偏好。 |
| `community/controller/` | 新增 `RankController.java`；改造 `SearchController` 保留旧接口兼容。 |
| `community/service/` | 新增 `PostRankService`、`SearchHotRankService`。 |
| `social/entity/User.java` + `UserVO.java` | 增加 `studentVerified`、`studentNoMask`。 |
| `verification/` 新包 | 新增认证 Entity、Mapper、Service、Controller。 |
| `task/RankRefreshTask.java` | 新增定时刷新任务。 |
| `db/migration/V3.8__rank_and_verification.sql` | 新增表与字段迁移。 |

---

## 11. 迁移脚本要点

文件：`campus_wall/src/main/resources/db/migration/V3.8__rank_and_verification.sql`

- 新增 `hot_rank_config` 表。
- 新增 `student_verification` 表。
- `user` 表增加 `student_verified`、`student_no`。
- 初始化热榜权重配置。

---

## 12. 验收标准

- [ ] AI 对话能根据用户偏好改变语气、风格、长度。
- [ ] 热帖榜支持日/周/月切换，榜单内容随窗口过期自动更新。
- [ ] 热搜榜支持日/周/月切换，旧搜索自动清除。
- [ ] 学生认证可上传正反面，提交后状态流转正常，人工复核通过后用户显示认证标识。
- [ ] 所有新增接口均有 Swagger/OpenAPI 注解。
- [ ] 前端页面样式符合项目 Kawaii 设计规范。
