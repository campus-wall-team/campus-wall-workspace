# ============================================================
# Campus Wall 前端 — 开发环境配置模板
#
# 用法：复制为 campus-wall-frontend/.env.development
#   cp deploy/team-dev/frontend.env.development.example campus-wall-frontend/.env.development
#
# VITE_API_BASE_URL 指向「你自己运行的后端」（不是中间件服务器）：
#   · H5 浏览器预览        → http://localhost:8080
#   · 微信开发者工具/真机   → 用你本机的局域网 IP，如 http://172.21.160.xx:8080
#       （微信真机访问 localhost 会失败，必须用局域网 IP；用 `ip addr` / `ifconfig` 查本机 IP）
#
# 如果你不想自己跑后端，也可以直接指向服务器上的「公共参考后端」：
#   VITE_API_BASE_URL=http://172.21.160.212:8080
# ============================================================

VITE_API_BASE_URL=http://localhost:8080
VITE_WS_BASE_URL=ws://localhost:8080
